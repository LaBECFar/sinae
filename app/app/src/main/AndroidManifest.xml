<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.webgenium.sinae">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".ExperimentoActivity"
            android:label="Experimento"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".NovoTesteVideoActivity"
            android:label="Vídeo do Teste"
            android:launchMode="singleTop"
            android:parentActivityName=".NovoTesteActivity" />

        <activity
            android:name=".NovoTesteActivity"
            android:label="Novo Teste"
            android:launchMode="singleTop"
            android:parentActivityName=".ExperimentoActivity" />

        <activity
            android:name=".NovoExperimentoActivity"
            android:label="Novo Experimento"
            android:launchMode="singleTop"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".MainActivity"
            android:label="Experimentos"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>